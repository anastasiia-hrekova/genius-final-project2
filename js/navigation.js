const button = document.querySelector('.main-page__btn');
const section = document.querySelector('.navigation');

const toggleOpen = () => section.classList.toggle('hidden');

button.addEventListener('click', toggleOpen);
button.addEventListener('click', toggleOpen);



     